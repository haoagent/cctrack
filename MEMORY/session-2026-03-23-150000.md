# Session Log: 2026-03-23 15:00–20:00

## Summary
Major cctrack feature session: ALL tab refactor, Todos panel, Stats (CLI + TUI panel), accurate pricing, extensive UX polish. 12 commits, pushed to GitHub.

## Key Decisions
- **ALL tab = session overview**: teams[0] always ALL, shows only top-level sessions (not team agents, not subagents)
- **Subagent detection**: Agent tool spawns detected by cwd+timestamp heuristic
- **Sessions vs Agents**: ALL tab says "Sessions", team tab says "Agents"
- **Todos replaces Tasks**: TodoWrite content shown live; Tasks panel removed (was empty for solo sessions)
- **ALL tab layout**: Sessions (left) + Stats (right) | Activity (bottom) — no Todos/Messages on ALL
- **Stats from transcripts**: Per-message daily attribution, not per-session date
- **Pricing verified against official Anthropic docs**: Opus $5/$25, Sonnet $3/$15, Haiku $1/$5; cache_write 5m=1.25x, 1h=2x, read=0.1x
- **Session naming**: clean transcript title > CWD dir > session_id (filter paths/commands)
- **User-facing labels**: in_progress → "running", completed → "done"
- **Product scope confirmed**: (1) Session overview + cost, (2) Team/subagent drill-down via Tab, (3) Stats (ccusage-like)

## Files Modified
- `src/stats.rs` — NEW: transcript scanner, per-model pricing, daily attribution, CLI output
- `src/tui/stats_panel.rs` — NEW: Stats panel for ALL tab (today/week/month/total + by project)
- `src/store/state.rs` — ALL tab refactor, unregistered sessions, subagent detection, todos, session naming
- `src/store/event.rs` — TodoUpdate event, todos field in TeamSnapshot
- `src/store/models.rs` — TodoItem model, fixed TokenUsage pricing to Opus defaults
- `src/collector/hook_server.rs` — richer summaries (7 tools), parse_todo_items, clean_session_title
- `src/tui/mod.rs` — Stats panel integration, 60s refresh, agent_count bug fix, status bar wording
- `src/tui/top_bar.rs` — ALL tab cyan bg
- `src/tui/agents_panel.rs` — dynamic "Sessions" vs "Agents" title
- `src/tui/tasks_panel.rs` — complete rewrite → Todos panel
- `src/tui/theme.rs` — cost_style() green
- `src/web/static/index.html` — tab bar, Todos panel, dynamic labels, stats header
- `src/web/static/app.js` — tab switching, Todos, token/cost columns
- `src/web/static/style.css` — tab bar styles, tool colors
- `src/lib.rs` — added stats module
- `src/main.rs` — cctrack stats subcommand
- `tests/store_test.rs` — updated for ALL-first model

## 12 Commits
```
b30a95c fix: empty project name for root directory
1c8453a fix: per-message daily token attribution for accurate Today/Week stats
75be798 fix: accurate pricing — correct model rates + 5min/1hr cache split
a0db43f fix: per-model pricing — opus/sonnet/haiku, correct cache_write rate
8e45f84 feat: Stats panel on ALL tab
131751e feat: cctrack stats — aggregate usage from all transcripts
c4aab16 fix: smart session naming — clean title > CWD dir > session_id
1d52722 fix: session name uses CWD directory name
e7424d5 feat: replace Tasks panel with Todos — live TodoWrite content
c3eaa8d ui: user-friendly task status labels
64498bc ui: context-aware labels — Sessions (ALL) vs Agents (team)
649be66 fix: ALL tab shows only top-level sessions, filter subagents
dc26d4f fix: ALL tab shows sessions not team agents
4eb1242 feat: ALL tab + session model refactor, drop solo, web UI sync
```

## Pricing Verification
Confirmed against https://platform.claude.com/docs/en/about-claude/pricing:
- Opus 4.6: $5/$25 input/output, cache 5m=$6.25, 1h=$10, read=$0.50
- cctrack vs ccusage: ~37% higher due to timing (live transcript growing) + possible LiteLLM pricing diff
- Today's session cost ~$35 (185 msgs, 54M cache_read tokens on Opus 1M context)

## Next Steps
- [ ] Investigate cctrack vs ccusage pricing gap more deeply (LiteLLM rates?)
- [ ] Web UI: add Stats panel for ALL tab (currently TUI only)
- [ ] Clean up dead Task code (Panel::Tasks enum, theme function names)
- [ ] GitHub + crates.io publish prep
- [ ] README / docs for open source release
